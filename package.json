{
  "name": "fragranza-web",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.3.1",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
